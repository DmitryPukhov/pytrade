from pytrade.connector.quik.WebQuikConnector import WebQuikConnector


class QuikFeed(WebQuikConnector):
    """
    Feed class.
    Just a placeholder, because WebQuikConnector plays role of both feed and broker.
    But in general feed and broker can differ
    """
